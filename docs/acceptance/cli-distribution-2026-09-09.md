# CLI distribution acceptance — 2026-09-09

Acceptance for [#31](https://github.com/monkeysees/small-cloud/issues/31), under specification #29. Linux x86-64 packaged checks pass, with [machine-readable evidence](../evidence/cli-distribution-2026-09-09.json); four-target/public-download acceptance is not yet complete.

The PyInstaller production executable was built on Debian 12 with Python 3.11.2 and no system package installation. Its shared Python library was extracted from the matching Debian package into temporary build storage. Release CI instead builds on Ubuntu 24.04 and macOS 15 for both ARM64 and x86-64; the documented Linux support floor follows those CI images.

The installed Linux x86-64 executable passed nine checks outside the checkout: bare invocation, help, version, catalog, bundled guide, unauthenticated status, both endpoint-flag forms, and hosted HTTPS with unavailable system CA paths. The last check uses an invalid synthetic credential and expects the service's authentication refusal; it reproduced a TLS failure before bundling certifi and passes with the bundle. Old environment/configuration endpoints and the fixture-origin environment variable did not redirect the executable; its protected state lock remained bound to the hosted origin. Executable checks run with no programs on PATH. Installation created no credentials, preserved shell startup files and reported the missing PATH entry. A corrupt download failed checksum verification and preserved the existing executable. This test served local build bytes at the download boundary; it does not establish public download availability.

The separately frozen internal test executable completed browser-protocol approval with the signed-provider HTTPS fixture, saved an owner-only file credential, reused it, refused changed origin metadata, and revoked/removed it on logout. A clean Debian container running D-Bus and GNOME Keyring also passed the packaged native Secret Service roundtrip, including explicit assertion of keyring storage. Both packages include their own Python runtime; Python outside the executable operates the supporting HTTPS fixture only. The actual production executable also successfully read hosted authentication status using the workstation's previously retained credential. No new Google approval or hosted mutation was needed for that read.

Independent Standards review reported zero findings. Spec review found no implementation defects or scope creep and identified the two outstanding acceptance areas below. The workflow passes `actionlint` 1.7.12, and the installer passes POSIX shell syntax checking.

The final full run passed all 86 identity tests in 317.629 seconds, all 89 infrastructure tests, and all four hosting-fixture tests. Typechecking passed for the production project and the distribution build/acceptance scripts. The identity suite includes the existing subprocess/HTTPS authorization, publishing, PostgreSQL, redeployment, sharing, secret and lifecycle checks using the source-only isolated-service launcher. No tests were skipped.

Remaining acceptance: execute the native workflow on all four OS/architecture targets, verify each native OS keyring roundtrip, publish all four production executables and installer at the approved download location, and execute the installer against those real HTTPS downloads. The fixture executable must remain undistributed. No four-platform, public-installation or human-walkthrough completion is claimed until those checks pass.
