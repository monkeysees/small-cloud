# CLI login acceptance — 2026-09-09

Acceptance for [#32](https://github.com/monkeysees/small-cloud/issues/32) is complete. Version `v0.2.0`, tagged at `c529a2e`, is available through the public installer and all four standalone targets. Automated and release acceptance passed. Real Google approval, retained status and logout passed for both combined and split login, with browser approval performed by the operator. See [machine-readable evidence](../evidence/cli-login-2026-09-09.json).

## Automated and native acceptance

The local regression suite passed all 96 identity, 89 infrastructure and four hosting-probe tests. Typechecking passed for production and distribution code. Standards and Spec reviews have no remaining findings; a consumed-delivery recovery error found during review was fixed and regression-tested.

The [release workflow](https://github.com/monkeysees/small-cloud/actions/runs/34377460688) built Linux x86-64/ARM64 and macOS Intel/Apple Silicon executables. Every target passed installed-executable checks and signed-provider HTTPS fixture journeys using both file credentials and its native OS store (Secret Service or Keychain). Those journeys cover combined login, split login and single-use delivery, pending timeout/resumption, protected pending state and origin binding, server expiry, retained status and logout. Fixture executables are excluded from downloads.

The [public installation workflow](https://github.com/monkeysees/small-cloud/actions/runs/34378149635) passed on all four native targets using real HTTPS downloads, without a source checkout or Python setup. Installation created no credentials and left shell startup files unchanged. All eight versioned binary/checksum URLs matched the verified artifacts; four `latest` checksum aliases matched after atomic activation. Version `v0.1.0` remains available.

## Hosted checks and remaining browser acceptance

A separate Ubuntu 24.04 container without Python installed v0.2.0 through the public installer. Its executable ran offline discovery with an empty PATH and reused a protected temporary copy of the retained hosted credential to report the current identity, initial workspace and no missing setup steps. The copy was removed after verification; the original credential was preserved. The host workstation's older glibc is below the documented standalone minimum, so hosted executable checks ran in the supported container.

Fresh combined and split login requests from the verified standalone executable reached the hosted browser-verification stage without terminal prompts. The operator approved both in their real Google browser session. Each CLI flow saved its credential, and subsequent hosted status and logout passed. After split logout, a direct status request using that test credential returned `CREDENTIAL_REVOKED`, and its local file was absent. The retained pre-existing credential remains valid. Live all-credential revocation affects existing sessions and has not been performed; automated fixture tests cover that behavior.

## Two-tab approval fix

The operator opened both approval links before submitting. The first form returned the exact `FORBIDDEN` CSRF/origin error while the second succeeded. The TLS fixture reproduced it deterministically: the second Google callback replaced the shared browser-session cookie, so the first form's CSRF value no longer matched. Submitting that same first form with its original cookie succeeded, isolating cookie replacement from origin and expiry.

Server fix `2de9ac4` retains a still-valid session only when it belongs to the freshly verified Google identity. It preserves the original server expiry. The two open forms now both approve; switching accounts still invalidates the old form, and forged CSRF/untrusted origins remain refused. All 99 identity, 89 infrastructure and four hosting-probe tests passed, as did typechecking and both review axes.

The two changed server files were deployed after verifying the installed baseline hashes, preserving a rollback copy. Local readiness and public authenticated status passed after restart. The v0.2.0 executable remains usable without reinstalling. The operator successfully approved a fresh split-login attempt after deployment, completing the remaining hosted journey.

Richer workspace selection remains #20, and broader fresh-agent/human workflow acceptance remains #45. This record does not claim those scopes complete.
