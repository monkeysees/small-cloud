# App publishing acceptance — 2026-09-09

Acceptance for [issue #5](https://github.com/monkeysees/small-cloud/issues/5), including the 2026-09-08 verification and 2026-09-09 operator confirmation. See the [publishing runbook](../../identity/PUBLISHING.md) for installation and operation.

[Recorded evidence](../evidence/publishing-2026-09-08.json) covers a real CLI upload, fresh CX23 builds in Nuremberg, verified image transfer, gVisor execution, creator-authenticated HTTPS, replacement of spoofed identity headers, private readiness denial, bounded build logs and builder deletion. A further publish with `PATH=/nonexistent` succeeded and retained the same app URL, proving the installed CLI needs no local Docker executable. Public runtime ports 18080–18085 were unreachable; app public HTTPS succeeded while metadata and private control/runtime connections were denied, with the corresponding listeners independently confirmed healthy from the runtime host.

The full local suite passed 115 tests before live fixes. A live image-import failure exposed an incorrect assumption that the exported config digest would equal Docker's imported image ID; the worker now validates the fixed export tag and pins Docker's actual immutable ID. That regression failed before its fix and passed afterward. The final affected suites passed six publishing HTTP tests, five gateway HTTP tests and seven upload CLI tests; Pyright reports zero errors. Independent Standards and Spec reviews have no unresolved findings. HEAD/bodyless response framing also has a passing HTTP regression.

The retained creator-only `publishing-probe` app is available at the URL in the evidence. The identity service, publishing worker and Caddy are installed and active; the workstation CLI is updated. Browser handoff, wrong-cookie/replay protection and Origin checks pass the local HTTP suite. A real browser connection was unavailable in this session, so live Google browser handoff is not claimed. Existing Google creator identity and retained credentials were verified against the deployed management service.

On 2026-09-09 the operator confirmed that all four live browser checks passed: creator Google sign-in, expected network results, public readiness returning 404, and denial of another admitted account. This resolves the earlier live browser verification gap; these outcomes are operator-reported.
