# Keep platform content and execution in the EU

Source, images, databases, secrets, and logs must be stored in EU regions, with builds and tools executing there. The pilot does not require all provider account metadata or support access to remain within the EU. Creator-configured external API calls may send data elsewhere, and their destinations are the creator's responsibility. This defines the residency requirement without implying control over external services chosen by creators.
