# Domain docs

This project uses a single-context layout.

Before exploring, read root CONTEXT.md and relevant decisions in docs/adr/.
Use glossary terms consistently in specifications, issues, code, and tests.
Surface conflicts with ADRs explicitly rather than silently overriding them.

If domain documents are missing, proceed without requiring their creation.
Use domain-modeling when terminology or architectural decisions need recording.
